name: Morning Scan

on:
  schedule:
    # 화~금 오전 08:30 KST (UTC 23:30 전일)
    - cron: '30 23 * * 1-4'  # 월~목요일 UTC (한국 화~금 아침)
    # 월요일 오전 08:30 KST (금요일 뉴스 포함 수집)
    - cron: '30 23 * * 0'    # 일요일 UTC (한국 월요일 아침)

  workflow_dispatch:  # 수동 실행 허용

jobs:
  morning-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run morning scan
        env:
          DART_API_KEY: ${{ secrets.DART_API_KEY }}
        run: |
          python src/morning_scan.py

      - name: Commit and push results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/
          git diff --staged --quiet || git commit -m "Update signals $(date +'%Y-%m-%d %H:%M')"
          git push
